# TODO.md

本文件只记录任务状态。实现历史见 `CHANGELOG.md`，最近交接见 `HANDOFF.md`。

## High Priority

- [x] 收敛v102功能线与main v96功能线并发布统一正式版本
  - 状态：已完成全分支/Worktree/不可达对象审计，建立两侧pre-integration archive标签，以v102为基线合入main，保留FTS5、8种排序、SHA/pHash、视频兼容、统一远程权限、04:00维护和图集延迟回收；正式发布、推送与临时引用清理按同一验收门禁完成。

- [ ] 活动Codex控制内核退出后移除最后一个旧Worktree
  - 状态：`8dbe`干净且HEAD已进入main，远程分支已删除；PID 8648当前以该目录为working-dir，按安全规则暂不强停、不强删。进程自然退出后执行`git worktree remove`，再删除本地`codex/media-library-cleanup`。

- [x] 建立带正式发布时间的版本标识和网页更新记录
  - 状态：自v102起使用`v<版本>-YYYYMMDD-HHmm`，设置页按需读取单一`release-notes.json`，页脚可进入；历史v99/v100/v101只补录已确认日期，不伪造时分。

- [x] 统一图册排序并增加上传图片精确哈希查找
  - 状态：v99已完成8种公共排序、后端分页前排序、旧配置兼容、搜索相关性保留，以及复用`media_hashes.sha256`的流式单图查询；数据库schema和正式媒体均未修改。
  - v100修复：上传格式改为签名优先，正常PNG不再因空/通用/冲突浏览器MIME或无扩展名而误拒绝；扩展名冲突会准确提示声明格式与真实格式。
  - v101完成：SHA-256精确查找旁新增标准64位pHash、紧凑BLOB表、手动单worker增量任务、暂停/继续/停止、480/512 MiB磁盘门槛和分级相似结果；正式全量索引需发布后按1000/10000张门槛分批推进。

- [x] 将媒体清理永久删除改为可恢复的项目回收站
  - 状态：v91已实现并部署同盘rename、跨盘copy-verify-delete、manifest幂等、冲突改名、ChangedSinceScan跳过与localhost恢复；隔离执行/API测试通过，正式媒体零移动，待用户手工确认。

- [x] 在设置页实现媒体库清理扫描、分页报告、停止和受控执行
  - 状态：v86扫描/报告已部署；v91废弃永久删除并改为项目回收站。批准报告jobId为`20260714-232613-22183b82`，截至v91部署验收正式移动文件和目录均为0。

- [x] 将媒体清理功能安全部署并推送正式main
  - 状态：功能分支先以普通merge集成v85，再由正式main使用`--ff-only`快进；没有冲突、rebase、force push或历史改写。功能Worktree和远程分支按要求保留。

- [ ] 根据第一次正式只读扫描体积制定媒体清理报告保留和容量告警策略
  - 说明：本次 11 个报告约 10.4 MiB，位于现有 Runtime logs，不创建新根目录；确认审计保留期限前不自动删除。

- [x] 实施 V2.0.1 移动端流量与按需加载优化
  - 说明：依据 `docs/V2.0.1_MOBILE_BANDWIDTH_AUDIT.md` 修复预览回退原图、轮播全量 src、请求取消、首页/媒体批次和缓存维护。
  - 状态：核心实现和隔离smoke完成；真实Chrome Network对比仍由下项跟踪。

- [ ] 使用浏览器 DevTools 补录 V2.0.1 优化前基线
  - 说明：移动端视口、Disable cache、导出 HAR，记录请求数、原图数、API/图片/总传输量和首屏时间。
  - 状态：Chrome、扩展和Native Messaging Host已验证可用；灯箱v85已通过受控Chrome页面资源与调度时间线验收，但Disable cache/HAR、节流和长期内存基线仍未补录，未虚构测量数据。

- [ ] 完成ZeroTier实体外部设备验收
  - 说明：批准`Configure ZeroTier Access.cmd`的UAC后，在同一ZeroTier网络的外部设备使用HTTP访问`http://192.168.192.1:48102/`。
  - 状态：UAC已批准，严格限定的Private规则已实际创建，服务器本机ZeroTier URL返回HTTP 200；实体外部设备结果不得由本机测试替代。

- [ ] 完成本机真实浏览器手工复验
  - 说明：依次打开127.0.0.1、localhost和LAN HTTP URL，记录地址栏最终协议与具体错误。
  - 状态：正式LAN URL已由真实Chrome完成滚动恢复专项回归；127.0.0.1与localhost地址栏协议复验仍待补充。

- [ ] 若48102再次停止，调查进程退出生命周期
  - 说明：保留PID、stdout/stderr、进程启动方式和停止时间；不要把无监听误判为页面故障。
  - 状态：v86部署通过现有脚本精确重启，当前Node PID `18852`由任务Host PID `23080`托管，状态为running。

- [ ] 完成PC和实体手机人工浏览器验收
  - 说明：检查首页、目录、图片、视频控件、搜索、收藏、最近浏览和设置页；ZeroTier/LAN需跨设备验证。
  - 状态：HTTP/API/媒体链路已通过；受控浏览器权限阻断，实体手机不在Codex控制范围。

- [ ] 以管理员权限确认48102 Private防火墙规则
  - 说明：运行`configure-firewall-48102.ps1`并核对ZeroTier网络类别，避免开放全部Public网络。
  - 状态：当前脚本因没有管理员令牌拒绝执行，系统未修改。

## Medium Priority

- [ ] 实现HLS异步按需任务与生命周期清理
  - 说明：增加播放器兼容评审、后台单视频任务、访问manifest、7天dry-run清理和容量门限。
  - 影响范围：服务端任务、Runtime运维和可能的播放器接入；不得同步阻塞视频请求。
  - 状态：V1.4.5只完成策略设计和环境变量，未生成或删除HLS。

- [ ] 设计可生产使用的全量缓存调度器
  - 说明：基于稳定分页清单、磁盘门限、批次游标、失败重试上限和人工观察窗口；默认不包含HLS。
  - 影响范围：Runtime运维脚本和容量管理，不修改媒体或数据库schema。
  - 状态：V1.4.4仅完成20图片/3poster小样本；禁止直接运行47万图片全量任务。

- [ ] 建立最小自动化 smoke check
  - 说明：覆盖语法、关键只读 API、缩略图和视频 Range，使用隔离数据。
  - 影响范围：测试脚本和维护文档。
  - 状态：待设计。

- [ ] 建立数据库备份与恢复演练
  - 说明：为新 runtime 定义停机备份、哈希、保留周期和恢复验证。
  - 影响范围：runtime 运维。
  - 状态：V1.4 实施后执行。

## Low Priority

- [ ] 增加运行数据容量统计和告警
  - 说明：监控数据库、日志、缩略图、poster 和 HLS 增长。
  - 状态：待 runtime 稳定后规划。

- [ ] 评估任务计划程序或服务管理器托管
  - 说明：仅在手工启动稳定、回滚演练完成后实施。
  - 状态：待确认。

- [ ] 评估可信网络之外的访问控制
  - 说明：项目当前无登录/角色/API 鉴权。
  - 状态：若部署范围扩大再处理。

## Done

- [x] 定位`利世/.../看球`视频为Chrome不支持的`mpeg4/mp4v`，实现并部署仅该图集点击触发、单路、无落盘H.264/AAC兼容流（前端`v97`；正式输出与停止进程验收通过）。
- [x] 将v97路径特例扩展为v98只读视频兼容性扫描、四级分类、增量报告、设置页和报告驱动的按需兼容播放；正式2096条全量与增量扫描完成。
- [x] 将FTS5 Integration V96最小方案部署到正式Runtime：一致性备份、474470行迁移、完整校验、`auto`模式、48102与六类API验收通过；未执行媒体扫描。

- [x] 完成并精简FTS5 Integration V96：保留mapped trigram、短词规则、严格搜索模式、媒体事务同步、最小状态、显式迁移/备份/quick-full校验和legacy回滚；停止生产级扩展，正式部署保持未执行。

- [x] 完成FTS5 Prototype V96第二阶段A：Node/SQLite实际能力、474470行完整副本多结构构建、URL解码相对路径、稳定mapping、两字媒体词、50k bigram小原型、正确性/计划/体积/资源/一致性与一冷五热基准；未接正式schema/API/扫描器或部署。
- [x] v96统一受信任LAN/ZeroTier管理写权限、Origin保护和能力接口；Explorer继续仅本机。
- [x] v96增加每日04:00幂等索引扫描、维护任务协调和繁忙重试。
- [x] v96增加末级纯媒体图集1小时撤销/整点回收队列、冲突不覆盖及网页操作日志。
- [x] v96移除所有目录/图集封面名称覆盖标签，保留下方标题和媒体类型功能徽标。

- [x] 完成搜索性能基线与低风险优化：真实执行计划、图集精确/前缀索引、总结果60上限、无COUNT/无媒体排序、开发态分段计时、250ms防抖、旧请求取消、乱序保护和30秒同词缓存；剩余`SCAN media`留待独立FTS5阶段评审（前端`v95`）。
- [x] 将正式`TRASH_DIR`调整为`E:\回收站`并精确重启；确认与`PHOTOS_DIR=E:\A_秀人`同盘、目标安全可写、v91/API/页面显示同盘rename，正式回收和恢复均未执行。
- [x] 修复正式Node重启后媒体清理历史报告不可见；v89只恢复最新有效报告用于查看，并保持恢复报告删除禁用；正式重启与浏览器验收已完成。
- [x] 将首页收藏/最近观看迁入设置子页，并把访问日志迁入SQLite，完成50条服务端分页、365天低频保留清理和旧NDJSON幂等导入（前端`v88`；正式Node后端已部署，实体iPad/iPhone复验尚未执行）。
- [x] 将灯箱当前原图改为独立P0立即通道；下一张P1提前解码，预测图P3延后，URL任务/Promise复用、低优先级缩略图和默认关闭的调试观测已用真实Chrome专项验证（前端`v85`；亚秒级快速连点仍由人工DevTools补测）。
- [x] 灯箱采用WebP即时预览、当前原图与后续最多3张的有界自适应预加载；并发2、缓存最多5项、下一张提前decode，关闭/换路由停止追加（前端`v81`）。
- [x] 修复`杏子yada/亮点`中伪装为`.jpg`的HEIC原图无法在Chrome灯箱显示；仅该图集灯箱使用既有WebP兼容预览，其他页面与原媒体不变（前端`v80`）。
- [x] 实现并用真实Chrome验证SPA滚动位置恢复、搜索历史状态和有界深层媒体补齐（前端`v79`）。
- [x] 首页亮点轮播加载全部20张有界WebP预览，并恢复每10秒向左自动推进一张（前端`v75`）。
- [x] 增加并验证响应式“回到顶部”悬浮按钮（前端 `v73`）。
- [x] 创建ZeroTier专用TCP 48102规则并验证LocalAddress、RemoteAddress、Profile和现有LAN规则不变。
- [x] 恢复物理LAN访问并验证LAN规则仅限`192.168.31.153`、`LocalSubnet`和TCP 48102。

- [x] V1.0 功能镜像迁移。
- [x] V1.0.1 迁移冻结补强。
- [x] V1.1 GitHub 工程标准化。
- [x] V1.2 文件和历史文档安全清理。
- [x] V1.2.5 轻度死代码清理。
- [x] V1.3 GitHub 发布和发布报告。
- [x] V1.4 运行环境只读审计和迁移计划。
- [x] V1.4.1 runtime 目录/env/启动器实施设计。
- [x] V1.4.2 独立 runtime、数据库哈希迁移和 PowerShell 运维脚本实施。
- [x] 修复 V1.4.2 启动器含空格路径参数。
- [x] V1.4.3 首次真实只读启动与 API/图片/视频 Range 验收。
- [x] V1.4.4修复poster新进程映射，并验证20图片/3poster可恢复小批量缓存任务。
- [x] V1.4.5冻结最终缓存策略、关闭新图片缩略图生成并完成Runtime容量/环境检查。
- [x] V1.5.0新增状态脚本、自动启动设计、运维手册和HTTP/API/网络地址验收。
- [x] 建立Windows双击启动/停止/状态入口，并安装当前用户登录后延迟30秒的自动启动任务。
- [x] 改为任务计划程序长期托管Node，完成关闭CMD后30秒、重复启动和任务化stop验收。
- [x] 将任务Host改为隐藏、非交互PowerShell，完成启动CMD自动退出/手工关闭、PID一致性、重复启动和精确停止验收。
- [x] 建立 Codex 长期上下文管理文档体系。

## Deferred

- [ ] 性能和加载优化
  - 原因：必须在 runtime 恢复并记录基准后进行。

- [ ] UI 和交互优化
  - 原因：当前阶段优先恢复稳定运行环境。

- [ ] 技术栈升级或模块拆分
  - 原因：现有原生技术栈可维护，暂无替换依据。

## Cancelled

- 当前没有已取消任务。

# V1.5.0 Runtime Takeover Acceptance Report

验收日期：2026-07-12

## Result summary

V1站点已作为日常运行候选启动，PID为`56500`，端口48102，Runtime和媒体路径正确，stdout正常、stderr为空。HTTP/API/媒体和收藏/最近浏览数据链路通过。

受控浏览器连接被当前Windows文件权限阻断，因此PC浏览器真实点击、视频控件播放和窄屏设备模拟未能由Codex自动完成。实体手机也不在Codex控制范围内。以下结果严格区分已验证和待人工确认，不把接口测试冒充浏览器测试。

## Automated HTTP and functional checks

| 项目 | 结果 |
|---|---|
| 首页 | HTTP 200，3,639 bytes |
| 本机地址 | `127.0.0.1:48102` HTTP 200 |
| LAN地址 | `192.168.31.153:48102` 本机HTTP 200 |
| ZeroTier地址 | `192.168.192.1:48102` 本机HTTP 200 |
| `/api/config` | HTTP 200 |
| `/api/index/stats` | HTTP 200 |
| `/api/collections/root` | HTTP 200 |
| `/api/highlights` | HTTP 200 |
| 搜索 | `/api/search?q=poster&limit=5` HTTP 200 |
| 原图 | HTTP 200，JPEG，1,057,519 bytes |
| 视频Range | HTTP 206，正确返回0–1023/30,484,072 |
| 设置入口 | hash入口HTML shell HTTP 200，正确引用`app.js` |

收藏与最近浏览分别执行临时记录的POST、GET确认和DELETE清理，均通过；最终没有保留`v1.5.0-acceptance-temp`测试记录。

## Browser acceptance status

| PC浏览器项目 | 状态 | 证据/待办 |
|---|---|---|
| 首页 | 接口通过，浏览器待人工确认 | HTML和静态入口200 |
| 目录 | API通过，浏览器待人工确认 | root collections正常 |
| 图片浏览 | 原图通过，浏览器待人工确认 | JPEG响应正常 |
| 原图访问 | 通过 | `/photos/...` HTTP 200 |
| 视频播放 | Range通过，控件播放待人工确认 | HTTP 206和Range headers正确 |
| 搜索 | API通过，浏览器待人工确认 | 搜索API 200 |
| 收藏 | 数据链路通过，浏览器待人工确认 | 临时写入/读取/删除通过 |
| 最近浏览 | 数据链路通过，浏览器待人工确认 | 临时写入/读取/删除通过 |
| 设置页面 | 路由入口通过，浏览器待人工确认 | SPA shell和`app.js`正常 |

浏览器自动化阻断原因：受控浏览器运行时无法读取当前用户AppData路径，连接阶段返回Windows `EPERM`。没有切换到未经允许的外部浏览器自动化工具。

## LAN and ZeroTier status

- LAN IPv4：`192.168.31.153`。
- ZeroTier IPv4：`192.168.192.1`。
- 站点监听`0.0.0.0:48102`。
- 本机通过两个地址访问均为HTTP 200。
- TCP 48102防火墙规则不存在。
- 执行项目防火墙脚本时，脚本因没有管理员令牌拒绝运行，未修改系统。

因此网络绑定和本机地址路由已通过，但实体手机跨设备访问仍为待确认。用户需在管理员PowerShell中显式运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\configure-firewall-48102.ps1
```

随后分别用手机访问：

```text
http://192.168.31.153:48102/
http://192.168.192.1:48102/
```

若ZeroTier网络类别不是Private，现有Private-profile规则仍可能不匹配，应先审计网络类别，不要直接开放所有Public网络。

## Current runtime state

- `scripts/status-gallery.ps1`：`running`。
- PID：`56500`。
- Node：运行中。
- 端口：48102 TCP连接探测成功。
- PID和日志位于`D:\GalleryRuntime\logs`。
- 本次未运行扫描、查重、删除、HLS或大规模缓存任务。

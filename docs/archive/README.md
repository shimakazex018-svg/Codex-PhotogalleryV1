# Archived Documentation

本目录保存从迁移源继承、但已经被当前 README、AGENTS、网页交接文档或 `docs/` 维护文档替代的历史说明。

归档文件不代表当前运行事实，不应直接用于修改代码或部署：

- `README-SERVER-WINDOWS.md`：旧 Windows 部署步骤，包含旧目录结构和过期端口说明。
- `HLS-PREVIEW-NOTES.md`：HLS 预研记录，包含旧机器示例路径。
- `SQLITE-INDEX-NOTES.md`：SQLite 过渡期说明，部分兼容 API 描述已失效。
- `ROADMAP-GALLERY-UPGRADE.md`：旧升级路线，其中多项功能已经实现。

需要追溯设计背景时可以阅读；需要确认当前行为时，应以源码、根目录 `README.md`、`网页.md`、`AGENTS.md` 和当前 `docs/` 为准。
